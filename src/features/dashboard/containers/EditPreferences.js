import React, { Component } from 'react'
import { connect } from 'react-redux'
import styles from '../dashboard.scss'

class EditPreferences extends Component {
    render() {
        return (
            <div >
                MAIN
            </div>
        
        )
    }
}

export default connect(null, null)(EditPreferences)
